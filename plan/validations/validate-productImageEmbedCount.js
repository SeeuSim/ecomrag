/**
* Validation code for field productImageEmbedCount on plan
* @param {import("gadget-server").PlanProductImageEmbedCountFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.productImageEmbedCount` to get the fields's data
  // add to the `errors` with `errors.add('productImageEmbedCount', "did not pass the validation")`
};