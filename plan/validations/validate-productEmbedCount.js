/**
* Validation code for field productEmbedCount on plan
* @param {import("gadget-server").PlanProductEmbedCountFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.productEmbedCount` to get the fields's data
  // add to the `errors` with `errors.add('productEmbedCount', "did not pass the validation")`
};